{
  "name": "runner-dash",
  "compatibility_date": "2024-11-12",
  "compatibility_flags": ["nodejs_compat"],
  "pages_build_output_dir": "./dist",
  // Default env variables for local development
  "vars": {},
  // Deployed environments (pages only supports preview and production)
  "env": {
    // Production
    "production": {
      "vars": {}
    }
  }
}
